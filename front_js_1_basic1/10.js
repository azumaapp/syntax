// calculator 객체(=클래스)는 plus에서 a + b를 반환한다. minus에서 a - b를 반환한다.
const math = {
    plus: function(a, b) {
        return a + b
    },
    minus: function(a, b) {
        return a - b
    },
    pi: 3.14
}

console.log(math.pi)
