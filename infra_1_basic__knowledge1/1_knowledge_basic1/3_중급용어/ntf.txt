[GraphQL]

Rest API의 경우는 요청으로 모든 데이터를 받아오지만,
GraphQL은 요청으로 원하는 정보만을 취할 수 있다.
즉, 같은 API를 쓴다하더라도, 원하는 정보가 사용자마다, 기기마다, 상황마다 다를 경우
효율적인 방식이 된다.

요청은 단순하지만 데이터는 복잡한 Rest API
요청은 복잡하지만 데이터는 효율적인 GraphQL
장단점이 있으므로 백엔드 서버 하나에 둘다 구현해놓고 둘중 원하는 것을 쓰면 된다.
GraphQL은 구현방법이 다양해서 구현에 어렵긴 함.
